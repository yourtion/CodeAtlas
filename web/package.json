{
  "name": "codeatlas-web",
  "version": "1.0.0",
  "description": "Web frontend for CodeAtlas",
  "main": "src/main.js",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "svelte": "^4.0.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.2.10",
    "@rsbuild/plugin-svelte": "^0.2.10"
  }
}